spring:
   datasource:
      name: crm
      url: jdbc:mysql://localhost:3306/elec-order?useUnicode=true&characterEncoding=UTF8&useSSL=false
      username: ENC(9397CYXMXkHpb5TVl6FlPw==)
      password: ENC(xQWl6FxysKQYa+flq+EwVg==)
      # 使用druid数据源
      type: com.alibaba.druid.pool.DruidDataSource
      driver-class-name: com.mysql.jdbc.Driver
      filters: stat
      maxActive: 20
      initialSize: 1
      maxWait: 60000
      minIdle: 1
      timeBetweenEvictionRunsMillis: 60000
      minEvictableIdleTimeMillis: 300000
      validationQuery: select 'x'
      testWhileIdle: true
      testOnBorrow: false
      testOnReturn: false
      poolPreparedStatements: true
      maxOpenPreparedStatements: 20
   jackson:
      date-format: yyyy-MM-dd HH:mm:ss
      time-zone: GMT+8

feign:  
  hystrix:  
    enabled: true
ribbon:
  ReadTimeout: 5000
  ConnectTimeout: 5000
  
#hystrix 配置
hystrix:
  command:
    default:
      execution:
        timeout:
        #如果enabled设置为false，则请求超时交给ribbon控制
          enabled: true
        isolation:
          thread:
            timeoutInMilliseconds: 5000

#如果想对单独的某个服务进行详细配置，如下
#poc-customer-service:
#  ribbon:
#    connectTimeout: 5000  
logging:
  level:
    com.poc: INFO
    jdbc.audit: ERROR
    jdbc.resultset: ERROR
    jdbc.sqlonly: ERROR
    jdbc.sqltiming: INFO
    jdbc.connection: ERROR

